# Photobooth app dependencies
# Install with: pip install -r requirements.txt --break-system-packages
# (picamera2 is normally installed via apt on Raspberry Pi OS / Debian
# Bookworm, not pip — see setup instructions.)

kivy>=2.3.0
numpy>=1.24
Pillow>=10.0
picamera2>=0.3.18
